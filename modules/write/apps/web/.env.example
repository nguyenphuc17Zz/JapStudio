# ============================================================
# Frontend environment template — copy to .env and edit locally.
# .env files are git-ignored; only .env.example is committed.
# ============================================================

# Base URL of the FastAPI backend (used by the API client)
VITE_API_BASE_URL=http://localhost:8000